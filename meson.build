project('getlaid', 'c',
  version : '0.1.0',
  license : 'MIT')

sdl2_dep = dependency('sdl2')
add_project_dependencies(
  sdl2_dep,
  language : 'c')

inc = include_directories('include')

subdir('include')
subdir('src')
